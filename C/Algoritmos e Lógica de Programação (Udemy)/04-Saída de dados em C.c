#include <stdio.h> // biblioteca de entrada e saida

/*
Tipo                                  placeholder de formatação

int (inteiro 16bit) ----------------- %d ou %i
long (inteiro 32bit) ---------------- %li
long long int (inteiro 64bit) ------- %lli
float (Real precisão simples) ------- %f
double (Real precisão dupla) -------- %lf
char (um único caracteres) ---------- %c
char [] (texto) --------------------- %s

*/

int main () {


    printf ("Bom dia");
    printf ("Boa noite");
    //Vai sair= "Bom diaBoa noite"

    // adicionando "\n" ira adicionar a quebra de linha
    printf ("Bom dia\n");
    printf ("Boa noite\n");
    /* Vai sair=    Bom dia  
                    Boa noite"*/
}

int main () {

    int x, y;
    x = 10;
    y = 20;
    printf ("%d\n, x")
    printf ("%d\n, y")

    return 0;
}